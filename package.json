{
  "name": "my-mern-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd client && pnpm dev",
    "install-all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
